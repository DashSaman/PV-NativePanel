# S03 PostgreSQL 18 rehearsal — PASSED

- Host: `testAmir5-3`
- Source commit: `02b02e0c2a0c2dbada04420b7a853adc08b000ab`
- Rehearsal start: `2026-08-27T21:51:43Z`
- PostgreSQL cluster: `18/main`
- PostgreSQL port: `5432`
- Production database name `pvnaive`: not created by rehearsal

## Verified gates

- Source commit fetched and 17 sensitive files matched trusted Git blob SHAs.
- Migration manifest verified.
- Bash syntax verified.
- S03 preflight regression passed.
- APT candidate / pipefail regression passed.
- Ubuntu 26.04 contract regression passed.
- `pvnaive_app` unsafe role flags all false.
- Migration 0001 applied and idempotent re-apply passed.
- Application health passed over TCP loopback as `pvnaive_app`.
- DB server endpoint `127.0.0.1:5432`; client endpoint `127.0.0.1`.
- Direct SELECT of RLS signing key denied.
- Encrypted age backup passed and checksum manifest verified.
- Restore drill passed.
- Restored ownership passed.
- Restored ACLs passed.
- Restored signing key verification passed.
- Destructive disposable rollback passed.
- PostgreSQL listeners were loopback-only on `127.0.0.1:5432` and `[::1]:5432`.
- Caddyfile checksum before/after remained `101884de2dd11cb9d276df8e72cd068bed50e4ec6eb4ebb477184dda7a86e8b1`.
- `CADDY_CHANGED=false`
- `SSH_CHANGED=false`
- `FIREWALL_CHANGED=false`

## Harness-only compatibility fixes used for rehearsal

Two rehearsal harness issues were corrected only in the disposable copied test source and are not production S03 changes:

1. PostgreSQL boolean text accepted both `f` and `false` forms for the role-flag assertion.
2. Rehearsal source/temp directories were made traversable/readable by the `pvnaive` OS user so `health.sh` and its pgpass file could be exercised as the real service account.

Production S03 itself deploys the DB release below `/opt/pvnaive/db/releases`, so it does not rely on the `/root` rehearsal path.

## Result

`S03_REHEARSAL_RESULT=PASSED`

This evidence authorizes the real S03-DATABASE stage to run from the untouched, verified source commit above. S03 must remain NEXT until the real stage and postflight both pass.
