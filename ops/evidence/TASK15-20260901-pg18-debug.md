# Task15 schema20 PostgreSQL18 gate debugging — 2026-09-01

Task15 remains **IN PROGRESS / NOT MERGED / NOT DEPLOYED**.

Clean Task15 PR #54 was created from exact current main with code tree `c2687c4c3fc0d702dbbb3ba67c5f1c75663ebbb1`. Local Go/Web/race/static gates were green, but GitHub CI database job failed. The failure was reproduced independently on a newly installed isolated PostgreSQL **18.6** worker cluster; Production was not used for testing.

Fresh PostgreSQL18 findings:

1. The migration test fixture contained 11 malformed UUID literals using an `8-4-4-12` grouping instead of canonical UUID grouping. These were repaired locally and shell/diff checks passed.
2. After that repair, the test progressed farther and exposed a second incorrect test expectation: it expected `first_connected_at` to be populated for a fixture service term whose `start_policy` is `on_creation`.
3. Repository accounting semantics only set `first_connected_at` when a term is actually in the pending `on_first_successful_connection` path. Therefore the `on_creation` assertion is a test bug, not permission to change accounting semantics. The corrected test must assert that this fixture does not fabricate `first_connected_at`.
4. The earlier nondeterministic race-winner bug was already fixed in the exact PR #54 code tree: the follow-up same-IP checks use whichever concurrent caller actually won instead of assuming lock acquisition order.

The PostgreSQL18 test must be rerun from a clean schema after the UUID and `first_connected_at` fixture corrections. Continue fixing only failures reproduced on PG18; do not merge Task15 until the exact published head passes PostgreSQL18 migration/concurrency proof plus normal CI, WS1 Exact Accounting and WS1 Pinned Forwardproxy.

A temporary workflow-assisted patch attempt was not accepted as release provenance. Any superseding Task15 PR must use a clean commit/tree without transient workflow ancestry and must match the independently verified source tree exactly.
