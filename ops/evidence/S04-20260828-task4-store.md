# S04 Task 4 auth store evidence

- Task: PostgreSQL-backed authentication store
- TDD RED: CI run `33124230607`, Go job failed on `undefined: NewStore` as expected while database/web remained green.
- Production store commit: `3ea15885603deb545980d3f9709a4716eb8aa37d`.
- Format/dependency helper completed successfully and self-removed.
- pgx is pinned to the selected v5 stable line by `go.mod`/`go.sum`.
- Store rejects malformed session/CSRF hash material before SQL and binds authenticated requests through a PostgreSQL transaction using the existing signed RLS context.
- This evidence commit intentionally triggers normal-owner CI; Task 4 is not considered GREEN until that CI completes successfully.
